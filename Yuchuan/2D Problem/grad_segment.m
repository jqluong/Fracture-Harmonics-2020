%% Computes a gradient OPERATOR G for discontinuously-stored functions over tri mesh

%INPUTS: 
    %V = vertices
    %F = faces
%OUTPUTS:
    %G = #3|F|-by-3|F| matrix 
%e.g. G*u = gradient vector

function [G] = grad_segment(V,F)









end